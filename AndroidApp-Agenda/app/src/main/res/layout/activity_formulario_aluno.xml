<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <EditText
            android:id="@+id/activity_formulario_aluno_nome"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_margin="8dp"
            android:autofillHints="name"
            android:hint="@string/activity_formulario_aluno_edit_text_nome_hint"
            android:inputType="textCapWords" />

        <EditText
            android:id="@+id/activity_formulario_aluno_telefone"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_margin="8dp"
            android:autofillHints="phone"
            android:hint="@string/activity_formulario_aluno_edit_text_telefone_hint"
            android:inputType="phone" />

        <EditText
            android:id="@+id/activity_formulario_aluno_email"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_margin="8dp"
            android:autofillHints="emailAddress"
            android:hint="@string/activity_formulario_aluno_edit_text_email_hint"
            android:inputType="textEmailAddress" />

    </LinearLayout>

</ScrollView>
